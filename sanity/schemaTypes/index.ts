import { bulletin } from "./bulletin";
import { sermon } from "./sermon";
import {
  ctaObject,
  seoObject,
  imageWithAlt,
  socialLink,
  navItem,
  navChildLink,
  richText,
} from "./objects";

export const schemaTypes = [
  bulletin,
  sermon,
  ctaObject,
  seoObject,
  imageWithAlt,
  socialLink,
  navItem,
  navChildLink,
  richText,
];
